import 'package:flutter/material.dart';

/// Les mêmes couleurs que celles du site tytoai.app, pour que l'app Flutter
/// ait exactement le même univers visuel. Si tu changes une couleur sur le
/// site, pense à la reporter ici aussi.
class TytoColors {
  static const nuit = Color(0xFF151C2C);
  static const nuit2 = Color(0xFF1F2940);
  static const fauve = Color(0xFFC99A55);
  static const brume = Color(0xFF98A1B6);
  static const vert = Color(0xFF7FB2A6); // le plan Pro
  static const urgence = Color(0xFFC9553F); // le mode urgence
  static const lune = Color(0xFFEDE7D6);
}
